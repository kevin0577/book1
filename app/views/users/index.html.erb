<div class="row">
	<div class="col-xs-3">
		<h2>User info</h2>
		<%= render "info", user: current_user %>
	</div>

	<div class="col-xs-9">
		<h1>Users</h1>
		<table class="table table-hover table-inverse">
			<thead>
				<tr>
					<th>image</th>
					<th>name</th>
				</tr>
			</thead>
			<tbody>
				<% @users.each do |u| %>
				<tr>
					<td><%= attahment_image_tag(u.user, :profile_image, :fill, 50, 50, fallback: "no-image-mini.jpg") %>
					</td>
					<td><%= u.name %></td>
					<td><%= link_to "show" user_path(u.id) %></td>
				</tr>
				<% end %>
			</tbody>
		</table>
	</div>
</div>